# маркет.код

Дипломный проект — SB1 — Доска объявлений

## Стек

Python 3.14, Django 6, DRF, PostgreSQL 16, Simple JWT, django-filter,
drf-spectacular, Pillow, WhiteNoise.
Версии зависимостей закреплены в `requirements/`.
Тесты и стиль: pytest, Black, isort, Flake8.

## Установка

Нужны Python 3.14 и PostgreSQL 16.

```bash
python3.14 -m venv .venv
source .venv/bin/activate
pip install -r requirements/dev.txt
cp .env.example .env
chmod 600 .env
```

Укажите секрет и данные своей базы в `.env`. Переменные оболочки имеют приоритет
над этим файлом.

Секретный ключ:

```bash
python3 -c "import secrets; print(secrets.token_urlsafe(48))"
```

## База

Пример создания локальной базы:

```sql
CREATE ROLE bulletin_board WITH LOGIN CREATEDB PASSWORD 'dev';
CREATE DATABASE bulletin_board OWNER bulletin_board;
```

Право `CREATEDB` нужно тестам: Django создаёт и удаляет `test_bulletin_board`.

```bash
python manage.py migrate
python manage.py createsuperuser
python manage.py collectstatic --noinput
python manage.py runserver 127.0.0.1:8000
```

Сайт: http://127.0.0.1:8000/
Swagger: http://127.0.0.1:8000/api/docs/

## Учётные записи

| Метод | Назначение |
| --- | --- |
| `POST /api/users/register/` | заявка на регистрацию, письмо со ссылкой |
| `GET /signup/confirm/<token>/` | подтверждение, создание записи |
| `POST /api/users/signup/resend/` | повторное письмо |
| `POST /api/users/token/` | вход, выдача access и refresh |
| `POST /api/users/token/refresh/` | обновление access |
| `POST /api/users/token/verify/` | проверка токена |
| `GET`, `PATCH /api/users/me/` | профиль, фото через multipart |
| `POST /api/users/set_password/` | смена пароля |
| `POST /api/users/reset_password/` | письмо восстановления |
| `POST /api/users/reset_password_confirm/` | новый пароль по ссылке |

Два последних метода доступны и без префикса: `/users/reset_password/`
и `/users/reset_password_confirm/`. Короткие адреса скрыты из схемы.

Учётная запись создаётся только после перехода по ссылке из письма: до этого
данные формы хранятся заявкой. Ссылка одноразовая и действует сутки. Ответ
на запрос восстановления одинаков независимо от того, существует ли адрес,
и не меняется при отказе почтового сервера.

После смены или восстановления пароля прежние access и refresh перестают
действовать: у записи хранится время последней смены, и токен, выпущенный
раньше, отклоняется.

Локально письма печатаются в консоль, в тестах — в память.

## Объявления

Пять рубрик: скрипты и код, фриланс, курсы и обучение, готовые решения, железо.

| Метод | Кто | Что делает |
| --- | --- | --- |
| `GET /api/ads/` | все | лента опубликованных, по четыре на странице |
| `GET /api/ads/<id>/` | с токеном | карточка |
| `POST /api/ads/` | с токеном | создать |
| `PATCH`, `DELETE /api/ads/<id>/` | автор или админ | изменить, удалить |
| `GET`, `POST /api/ads/<id>/reviews/` | с токеном | отзывы и вопросы |
| `GET /api/categories/` | все | рубрики |
| `GET /api/discussions/` | с токеном | лента вопросов |

Список объявлений доступен и по короткому адресу `GET /ads/`.

Отбор: `?search=` по части названия, `?category=`, `?price_min=`, `?price_max=`,
`?ordering=` по цене или дате. Вопросы отделяются от отзывов параметром `?kind=question`,
ответ указывает `parent`.

Объявление: название, целая неотрицательная цена, описание, рубрика, фотография.
Фото необязательно: JPEG, PNG или WebP до 5 МБ, сервер проверяет и расширение,
и содержимое файла.

Состояния: на рассмотрении, опубликовано, снято с публикации. Перевести
в «опубликовано» может только администратор; при публикации автору уходит письмо.
Изменение содержимого снова отправляет карточку на рассмотрение. Чужое
неопубликованное объявление отдаёт 404 — существование скрытых записей
не раскрывается.

## Страницы

| Адрес | Что это |
| --- | --- |
| `/` | лента, поиск, по четыре карточки |
| `/category/<рубрика>/` | объявления рубрики |
| `/ad/<id>/` | карточка, отзывы и вкладка вопросов |
| `/ad/new/`, `/ad/<id>/edit/` | создать и править своё |
| `/ad/<id>/status/` | сменить состояние |
| `/ad/<id>/delete/` | удалить с подтверждением |
| `/ad/<id>/message/` | первое сообщение автору |
| `/messages/`, `/messages/<id>/` | диалоги |
| `/discussions/` | лента вопросов |
| `/account/` | свои объявления с отбором по состоянию |
| `/account/review/` | очередь на подтверждение, администратору |
| `/signup/`, `/login/`, `/logout/` | регистрация, вход, выход |
| `/signup/check-email/` | «проверьте почту» после заявки |
| `/signup/confirm/<token>/` | подтверждение, создание записи |
| `/signup/resend-verification/` | отправить письмо повторно |
| `/password-reset/` | восстановление пароля |
| `/about/` | о проекте |

Страницы работают на сессиях Django, API — на токенах. Опубликованную карточку
на сайте видит и гость; тот же метод API без токена не откроет.

## Переписка

Кнопка на карточке объявления, список диалогов, счётчик непрочитанного в шапке.
Диалог привязан к объявлению, участников двое, посторонний получает 404 —
в том числе администратор. Повторное обращение открывает тот же диалог.
Сообщение до 2000 символов, пустое не принимается. Не чаще десяти сообщений
в минуту и одного нового диалога в тридцать секунд. Снятие объявления
с публикации переписку не прерывает, удаление объявления или участника
удаляет её вместе со связанными записями. Отдельных методов API и писем нет.

## Настройки и ограничения

Полный список переменных — в `.env.example`.

| Группа | Зачем |
| --- | --- |
| Приложение | режим, секрет, хосты, адрес сайта, логи |
| Браузер | CSRF и CORS |
| PostgreSQL | имя, пользователь, пароль, хост, порт, SSL |
| JWT | срок access и refresh |
| Почта | сервер, логин, пароль, отправитель |
| Кэш | общее хранилище счётчиков, необязательно |

Ограничения частоты, всё за минуту:

| Что | С адреса | С учётной записи |
| --- | --- | --- |
| вход | 20 | 10 |
| регистрация | 20 | — |
| запрос восстановления | 20 | — |
| подтверждение по ссылке | 30 | — |

Письмо учётной записи уходит не чаще одного раза в минуту.
Сообщения — десять в минуту, новый диалог — один в тридцать секунд.

Счётчики живут в кэше приложения. Без переменной `DJANGO_CACHE_URL` это память
процесса: при нескольких рабочих процессах у каждого будет свой счёт. Для общего
ограничения укажите хранилище — поддерживается таблица в той же PostgreSQL,
её создаёт `python manage.py createcachetable`.

В журнал не попадают пароли, токены и ссылки из писем: чувствительные значения
вырезаются перед записью.

## Обслуживание

```bash
python manage.py purge_signup_requests
python manage.py cleanup_media
```

Первая удаляет заявки на регистрацию, по которым не перешли по ссылке.
Вторая показывает изображения, на которые не ссылается ни одна запись;
без подтверждающего ключа она ничего не удаляет, а только перечисляет кандидатов
и пропускает файлы моложе суток.

## Проверки

```bash
make lint
make test
make cov
```
